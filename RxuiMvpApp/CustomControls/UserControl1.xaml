<UserControl x:Class="RxuiMvpApp.CustomControls.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:esri="http://schemas.esri.com/arcgis/runtime/2013"
             xmlns:local="clr-namespace:RxuiMvpApp.CustomControls" xmlns:local1="clr-namespace:RxuiMvpApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local1:MapViewModel x:Key="MapViewModel" />
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <Button x:Name="ZoomInButton" Content="Zoom In" />
            <Button x:Name="ZoomOutButton" Content="Zoom Out" />
            <TextBox x:Name="Input1" Background="LightGoldenrodYellow" />
            <TextBlock x:Name="Label1"  />
            <Slider x:Name="SliderInput1" Minimum="0.0001" Maximum="193194680" IsMoveToPointEnabled="True" />
            <esri:MapView x:Name="MainMapView" Height="250"
                      Map="{Binding Map, Source={StaticResource MapViewModel}}" />
        </StackPanel>

    </Grid>
</UserControl>
